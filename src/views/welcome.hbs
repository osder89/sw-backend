<div class="container p-4">
    <div class="row">
        <div class="col-md-6 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Welcome {{user.username}}</h3>
                    <h4>{{user.correo}}</h4>
                    <a href="/salas" class="btn btn-primary m-4">
                        Conectatate a la pizarra mas optima del mercado    
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>